import React from 'react';
import logo from '../../img/logo.png';

export const Header = () => {
  return (
    <header className="center">
      <img src={logo} alt="Logo from Breaking Bad" />
    </header>
  );
};
export default Header;
